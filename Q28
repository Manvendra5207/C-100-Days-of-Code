#include <stdio.h>

int main() {
    int n, sum=0, i=0, count=1 ;
    
    printf("Enter value of n: ");
    scanf("%d",&n);
    
    while(count<=n){
        sum = sum+i;
        i = i+2;
        count++;
    }
    printf("Sum of %d even numbers is %d \n",n,sum);
    return 0;
}
